{
  "api_config": {
    "protocol": "http",
    "host": "localhost",
    "port": 3000,
    "prefix": "",
    "routes": {
      "list": "/posts",
      "get": "/posts/{id}",
      "create": "/posts",
      "update": "/posts/{id}",
      "delete": "/posts/{id}"
    }
  }
}
